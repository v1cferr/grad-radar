# O venv é Linux-específico e vive num volume do container — copiá-lo do host
# quebraria os binários. Os outros são caches sem valor dentro da imagem.
.venv/
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/
.mypy_cache/
.env
.env.*
