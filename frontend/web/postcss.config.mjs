// Tailwind v4 ships as a PostCSS plugin — there is no tailwind.config.js by
// default; configuration lives in CSS (see src/app/globals.css).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
